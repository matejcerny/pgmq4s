{%
laika.title = Classic Tagless Final
%}

# Classic Tagless Final

The classic tagless final style passes `PgmqClientF[F]` explicitly, making dependencies visible in type signatures and compatible with any `F[_]` effect type.
